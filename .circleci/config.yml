version: 2.1
jobs:
  build:
    docker:
      - image: cypress/browsers:node12.14.1-chrome85-ff81
    steps:
      - checkout
      - run: node --version
      - run: npm install
      - run: npm run test

workflows:
  build_and_test:
    jobs: 
      - build
      # - test
